# No runtime secrets exist for this app — zero third-party API dependency,
# zero cloud database, client-side-only PDF/QR generation (security-design.md
# "Secrets Management"). This file is intentionally empty/near-empty; it
# exists so `.gitignore`'s `.env*` exclusion has a tracked counterpart and
# so any future environment variable has a documented place to be added.
