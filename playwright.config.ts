import { defineConfig, devices } from '@playwright/test';

/**
 * Playwright E2E configuration — Chromium/Firefox/WebKit desktop plus
 * mobile viewports (team.md NFR3.1: "attendees may open the shared card
 * link on mobile, not desktop-only"). Exercised in Build and Test /
 * ci-pipeline (post-merge Gate 2), not in this Code Generation stage,
 * per unit-test-instructions.md.
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },
    { name: 'webkit', use: { ...devices['Desktop Safari'] } },
    { name: 'mobile-chrome', use: { ...devices['Pixel 7'] } },
    { name: 'mobile-safari', use: { ...devices['iPhone 14'] } },
  ],
  webServer: {
    command: 'npm run build && npm run start',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
